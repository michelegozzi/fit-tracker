<!-- 9.25 -->
<% provide(:title, 'All users') %>
<h1>All users</h1>

<!-- 9.34 -->
<%= will_paginate @users %>

<ul class="users">
	<%= render :partial => "user", :collection => @users %>
</ul>

<%= will_paginate @users %>