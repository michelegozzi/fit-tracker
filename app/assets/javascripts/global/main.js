jQuery(document).ready(function() {

    //jQuery( "#menu" ).menu();


	//jQuery('table.activities').addClass('table table-bordered table-hover');
/*
jQuery('table.activities').each(function() {

		jQuery(this).addClass('fit-table');

		jQuery(this).children('thead tr').first().addClass('fit-table-header');
		jQuery(this).children('tbody tr').each(function() { jQuery(this).addClass('fit-table-body'); });
		jQuery(this).children('tbody tr.footer').addClass('fit-table-footer');

	});


	
	

	jQuery('table.meals').addClass('fit-table');
	jQuery('table.meals > thead tr').first().addClass('fit-table-header');
	jQuery('table.meals > tbody').each(function() { jQuery(this).addClass('fit-table-body'); });
	jQuery('table.meals > tbody tr.footer').addClass('fit-table-footer');
*/
	
});